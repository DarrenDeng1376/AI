# Environment Configuration Template
# Copy this file to .env and fill in your Azure credentials

# Azure Document Intelligence Configuration
# This service extracts text, tables, and structure from documents (PDFs, images, Office docs)
AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=https://your-document-intelligence-resource.cognitiveservices.azure.com/

# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://your-openai-resource.openai.azure.com/
AZURE_OPENAI_API_VERSION=2024-02-01
AZURE_OPENAI_DEPLOYMENT_NAME=your_gpt4_deployment_name
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=your_embedding_deployment_name

# Authentication Method (choose one):
# Option 1: Use Azure Default Credentials (recommended for Azure-hosted apps)
# This uses Azure CLI login, Managed Identity, or other Azure authentication methods
AZURE_USE_DEFAULT_CREDENTIALS=true

# Option 2: Use API Keys (for local development or non-Azure environments)
# AZURE_USE_DEFAULT_CREDENTIALS=false
# AZURE_DOCUMENT_INTELLIGENCE_KEY=your_document_intelligence_key_here
# AZURE_OPENAI_API_KEY=your_openai_api_key_here

# Application Settings
VECTOR_STORE_PATH=./data/vector_store
MAX_FILE_SIZE_MB=50
CHUNK_SIZE=1000
CHUNK_OVERLAP=100
MAX_CHUNKS_PER_DOCUMENT=500
ALLOWED_FILE_TYPES=pdf,png,jpg,jpeg,tiff,bmp,docx,xlsx,pptx,txt
MAX_DOCUMENTS_PER_SESSION=20

# Performance Settings
MAX_CONCURRENT_REQUESTS=10
REQUEST_TIMEOUT_SECONDS=300
CACHE_TTL_SECONDS=3600

# Logging
LOG_LEVEL=INFO
LOG_FILE=./logs/app.log

# Streamlit Configuration
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=localhost
